"use strict";

let program = "year up";
let message = "Learning web development is awesome.";
let wholeMessage = program + message;
wholeMessage= wholeMessage.trim();
wholeMessage= wholeMessage.toUpperCase();

console.log(wholeMessage);
